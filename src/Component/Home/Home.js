import React from 'react';
import Login from '../Login/Login';
import Box from '@mui/material/Box';

const Home = () => {
    return (
        <Box>
            <Login></Login>
        </Box>
    );
};

export default Home;